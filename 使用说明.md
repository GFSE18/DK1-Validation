# TS20 模型 v2 使用说明

双击本文件夹的 **Start_Tests.cmd**。选择站立、连续蹲起、单腿站立或走路后，会打开 MuJoCo 并自动运行。运行结束后打开数据报告。选择“全部测试”会后台运行，不弹出仿真窗口。

主要调参位置：在 `build_model.py` 修改 `TORSO_MASS_KG` 可调整躯干重量；在 `run_tests.py` 修改 `MOTION_TIME_SCALE` 可调整控制轨迹速度，当前为 `0.80`，即比原方案快约 25%。

原版 `ts20_humanoid_500mm.xml` 保留。新版是 `ts20_humanoid_v2.xml`，需要配合动作程序运行；仅把新版 XML 拖入普通 Viewer 不会启动平衡控制。

## 已实现的动作

|动作|测试安排|检查内容|
|---|---|---|
|站立|8 秒|机身倾角、重心和脚部跟踪、接触|
|连续蹲起|1.6 秒准备，3 次 6.4 秒蹲起|每次骨盆下降至少 25 mm，并完成全时长|
|单腿站立|按加速后的轨迹移重心到左脚，右脚抬起约 25 mm，再放回|真实接触力和离地高度，单腿支撑至少 2 秒|
|缓慢走路|4 次交替摆腿，每次 6.4 秒；每只摆动脚前移 35 mm|每次摆腿真实离地支撑至少 0.5 秒，总前进至少 45 mm|

测试从轻微屈膝姿态开始，不是锁膝站直。动作时间已统一缩短到原来的 80%，连续蹲起的目标骨盆下沉量约 50 mm。默认步态仍是慢速、准静态实验控制，不是通用动态步行器。默认质量为估计的 4.11 kg，其中躯干为 2.0 kg。

控制器以 100 Hz 计算重心、机身姿态、脚部位置和接触力分配；MIT 形式的关节反馈以 1 ms 仿真步长执行。只向 23 个关节施加力矩，没有固定机身、牵引绳、外部基座辅助力或在运动中直接重写姿态。足部接触由 MuJoCo 实际求解。控制规划使用摩擦系数 0.6，仿真地面采用原模型 0.8，均为假设。

姿态、质量分布、惯量、阻尼、关节行程均未实机校准。机器人自身碰撞仍关闭，因为当前轴心重合的数字骨架还不是可装配 CAD。

## 扭矩和功率报告

每次运行会在 `results/时间_动作_策略/` 新建一个目录，保留旧结果：

- `report.html`：选择关节查看扭矩、速度、机械功率和角度曲线，下面有统计表。
- `timeseries.csv`：100 Hz 明细，包含实际与请求扭矩、目标与实际角度/速度、脚底力、超额定/限幅标志。
- `joint_statistics.csv`：峰值、RMS、最大速度、最大绝对机械功率、超额定时间、限幅时间和正负机械能。
- `summary.json`：动作成功/失败原因、轨迹误差、离地与前进证据、仿真限制。
- `replay.npz`：100 Hz 实际状态记录，可用 `render_replay.py` 从记录中生成截图。
- `model.xml`、`run_tests.py`、`motor_config.json`：此次模型和控制配置快照，便于追溯。

统计使用每个 1 ms 步的值，覆盖整个测试，含开始准备和结束回位；CSV 为 100 Hz，网页曲线为 20 Hz，所以稀疏曲线不一定显示全部瞬时峰值。角度跟踪曲线是控制器滚动生成的 MIT 局部目标，不能替代重心/脚部轨迹误差检查。

机械功率为输出端扭矩乘角速度。正功率是输出机械能，负功率是吸收机械能；负值不代表电池必然回充。不能据此直接推算电池耗电或续航。

## 电机规格与限制策略

来源：用户提供的 `KK-servo-datasheet-v0.1.pdf` 第 3、5 页。

- TS20-50：0.7 Nm 额定，2 Nm 峰值 @300 ms，31.4 rad/s 最大空载转速。
- TS20-100：1.5 Nm 额定，4 Nm 峰值 @300 ms，15.7 rad/s 最大空载转速。
- MIT 控制：τ = Kp(目标角度−实际角度) + Kd(目标速度−实际速度) + 前馈扭矩。
- `motor_config.json` 中可调整 Kp、Kd，程序检查其是否在规格书指令范围内；默认 12 和 0.4 是调试值，不是实测值。

**默认策略 rated**：直接限制在额定扭矩，不使用短时峰值。这不证明电机在任何速度、环境温度下都能连续输出额定扭矩；真实带载曲线和热条件仍未知。

可选 **peak-budget**：单次运行中，每个电机累计超过额定扭矩最多约 300 ms，此后锁定在额定扭矩，不自动恢复峰值。它是保守的试验策略，不是厂家热模型或真实固件降额逻辑。重新运行会重置预算，不表示实物电机已经冷却。

空载转速只用于越界提示，没有将它伪装为带载扭矩—速度包络。当前也没有效率、温升、热恢复或反电动势模型。

`upgrade_model.py` 从原版生成新版 XML 和默认电机配置；再次运行会重置这两个文件中的手动编辑。日常启动测试不运行生成器。

## 实机参数校准：已提供入口，未完成实物校准

选择菜单 7 可生成空模板。不要把示例或仿真数据当成实测值。

1. 把 `calibration/physical_measurements.template.json` 复制为 `physical_measurements.json`。
2. 填写测量来源；按每个刚体填写质量 kg、局部 BODY 坐标系中的质心 m、关于质心的六分量惯量 kg·m²。
3. 顺序为 Ixx、Iyy、Izz、Ixy、Ixz、Iyz；非对角项是惯量矩阵项，不能未经核对就使用 CAD 中另一符号约定的“惯性积”。
4. 导入器检查数值、正定性、主惯量三角条件，并编译验证后生成 `ts20_humanoid_measured.xml`。没有测量值时拒绝生成“已校准模型”。部分导入会列出剩余未测量刚体。

还提供单轴刚性台架拟合：输入真实记录的时间、角度、速度、输出扭矩和已知外部阻力矩，拟合总输出惯量、黏性摩擦和库仑摩擦。需要正反向、多速度、多加速度激励；不适用于直接把整机运动日志当作单轴台架。

拟合的总惯量包含夹具/负载，不能直接填进 MuJoCo 的电机 armature，否则可能重复计算负载。输出是待独立实验验证的候选参数，不会自动宣称完成硬件校准。还缺扭矩读数准确性验证、控制延迟、效率、带载速度和热参数。

## 机械安装检查：包络筛查，不是装配通过

菜单 6 生成 `installation/安装检查.md`、JSON 明细，以及可在 Viewer 打开的 `motor_envelopes.xml`（红色透明电机占位）。

默认对所有 23 个电机采用 Ø20×35 mm 圆柱，中心置于关节轴心。髋、肩、踝的轴心重合会产生包络冲突。算法使用有符号距离及内接球相交检测，避免完全重合圆柱被漏报。

可在 `motor_placements.template.json` 中填入真实的安装刚体、中心和轴向，重新筛查站姿和浅蹲姿态。即使这两个姿态没有电机互相冲突，也不代表完成整机机械验证。尚缺电机与支架/连杆干涉、全行程扫掠、线缆弯折、法兰与紧固件、轴承载荷和制造公差。

现有 `TS20.SLDPRT` 是单个零件；本工具没有解析该文件，也没有真实机器人装配模型。要完成安装验证，需要整机装配（含坐标、轴间偏移和安装件），通常可提供 STEP/装配模型和关节定义。

## 命令行（需要自定义时）

在此目录打开 PowerShell：

```powershell
$robotPython = 'C:\Users\Foots\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $robotPython -X utf8 run_tests.py --motion squat --viewer
& $robotPython -X utf8 run_tests.py --motion walk --policy peak-budget
& $robotPython -X utf8 calibration.py --apply-physical calibration/physical_measurements.json
& $robotPython -X utf8 run_tests.py --motion squat --model ts20_humanoid_measured.xml
& $robotPython -X utf8 calibration.py --fit-bench calibration/bench_measurements.csv
& $robotPython -X utf8 installation_check.py --placements installation/motor_placements.json
```

若换电脑，需安装 Python、mujoco、numpy、scipy、osqp、jinja2、joblib，并调整启动器中的 Python 路径。当前依赖安装在本目录 `.validation-deps`，没有要求使用系统 Python。
